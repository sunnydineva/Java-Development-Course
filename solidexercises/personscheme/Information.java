package solidexercises.personscheme;

public interface Information {

    void describePerson(Person person); //in Person class

    void tellAddress(Person person); //in abstract Person abstract tellAddress

    void playStatus(Child child); //in Child interface

    void workStatus(Adult child); //in Adult interface

}
